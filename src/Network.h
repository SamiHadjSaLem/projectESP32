const char *ServerName = "fileserver";

// IPAddress local_IP(10, 1, 110, 25); // Set your server's fixed IP address here
// IPAddress gateway(10, 1, 110, 1);  // Set your network Gateway usually your Router base address
// IPAddress subnet(255, 255, 255, 0);     // Set your network sub-network mask here
// IPAddress dns(10, 1, 200, 1);           // Set your network DNS usually your Router base address

IPAddress local_IP(192, 168, 1, 10); // Set your server's fixed IP address here
IPAddress gateway(192, 168, 1, 1);   // Set your network Gateway usually your Router base address
IPAddress subnet(255, 255, 255, 0);  // Set your network sub-network mask here
IPAddress dns(193, 95, 57, 20);      //set your network dns address

const char ssid_1[] = "GLOBALNET";
const char password_1[] = "mahdia2019";

const char ssid_3[] = "ETUDIANT_Plus";
const char password_3[] = "EnetcomEtud";

const char ssid_2[] = "HUAWEI Y7 Prime";
const char password_2[] = "hedhoud123";


const char ssid_4[] = "O-TURKI";
const char password_4[] = "01234567";